class World

operations

public run : () ==> ()
run() == (
	start(System`wt3_controller);
	block();
);

private block : () ==>()
block() ==  skip;

sync per block => false;

values
	--placeholders for SDPs
	public wt3_max : real = 0.0;
	public wt3_min : real = 0.0;

end World